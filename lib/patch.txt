--- a/adb_manager.dart
+++ b/adb_manager.dart
@@ -87,7 +87,7 @@ class AdbManager extends ChangeNotifier {
     final privPem = await _storage.read(key: _privateKeyKey);
     final pubPem = await _storage.read(key: _publicKeyKey);
     try {
-      if (privPem != null &&
-          privPem.isNotEmpty &&
-          pubPem != null &&
-          pubPem.isNotEmpty) {
+      if (privPem != null &&
+          privPem.trim().isNotEmpty &&
+          pubPem != null &&
+          pubPem.trim().isNotEmpty) {
